name: Download Telegram Bot Logs

on:
  workflow_dispatch:
  
  # Optionally, you can schedule periodic log downloads
  # schedule:
  #   - cron: '0 5 * * *'  # Daily at 5 AM UTC

jobs:
  download-telegram-bot-logs:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      
      - name: Install dependencies
        run: |
          sudo apt-get update && sudo apt-get install -y sshpass
          node --version
          npm --version
      
      - name: Check Required Environment Variables
        run: |
          echo "🔍 Checking required environment variables..."
          MISSING_VARS=""
          
          # List of required variables
          REQUIRED_VARS=(
            "SYSTEM_TELEGRAM_BOT_TOKEN"
            "DEEP_ASSISTANT_HEADQUATERS_TELEGRAM_CHAT_ID"
            "DEEP_ASSISTANT_HEADQUATERS_TELEGRAM_LOGS_TOPIC_ID"
            "TELEGRAM_BOT_SERVER_USER"
            "TELEGRAM_BOT_SERVER_PASSWORD"
            "TELEGRAM_BOT_SERVER_HOST"
            "TELEGRAM_BOT_SERVER_PORT"
            "TELEGRAM_BOT_SERVER_ROOT_PATH"
          )
          
          for var in "${REQUIRED_VARS[@]}"; do
            if [ -z "${!var}" ]; then
              MISSING_VARS="$MISSING_VARS $var"
              echo "❌ Missing: $var"
            else
              echo "✅ Found: $var"
            fi
          done
          
          if [ ! -z "$MISSING_VARS" ]; then
            echo ""
            echo "❌ Missing required environment variables:$MISSING_VARS"
            echo "Please configure these secrets in your GitHub repository settings."
            echo "Run: node scripts/configure-github-secrets.mjs"
            exit 1
          fi
          
          echo "✅ All required environment variables are configured"
        env:
          SYSTEM_TELEGRAM_BOT_TOKEN: ${{ secrets.SYSTEM_TELEGRAM_BOT_TOKEN }}
          DEEP_ASSISTANT_HEADQUATERS_TELEGRAM_CHAT_ID: ${{ secrets.DEEP_ASSISTANT_HEADQUATERS_TELEGRAM_CHAT_ID }}
          DEEP_ASSISTANT_HEADQUATERS_TELEGRAM_LOGS_TOPIC_ID: ${{ secrets.DEEP_ASSISTANT_HEADQUATERS_TELEGRAM_LOGS_TOPIC_ID }}
          TELEGRAM_BOT_SERVER_USER: ${{ secrets.TELEGRAM_BOT_SERVER_USER }}
          TELEGRAM_BOT_SERVER_PASSWORD: ${{ secrets.TELEGRAM_BOT_SERVER_PASSWORD }}
          TELEGRAM_BOT_SERVER_HOST: ${{ secrets.TELEGRAM_BOT_SERVER_HOST }}
          TELEGRAM_BOT_SERVER_PORT: ${{ secrets.TELEGRAM_BOT_SERVER_PORT }}
          TELEGRAM_BOT_SERVER_ROOT_PATH: ${{ secrets.TELEGRAM_BOT_SERVER_ROOT_PATH }}

      - name: Download and Upload Telegram Bot Logs to Telegram
        env:
          SSHPASS: ${{ secrets.TELEGRAM_BOT_SERVER_PASSWORD }}
          SYSTEM_TELEGRAM_BOT_TOKEN: ${{ secrets.SYSTEM_TELEGRAM_BOT_TOKEN }}
          DEEP_ASSISTANT_HEADQUATERS_TELEGRAM_CHAT_ID: ${{ secrets.DEEP_ASSISTANT_HEADQUATERS_TELEGRAM_CHAT_ID }}
          DEEP_ASSISTANT_HEADQUATERS_TELEGRAM_LOGS_TOPIC_ID: ${{ secrets.DEEP_ASSISTANT_HEADQUATERS_TELEGRAM_LOGS_TOPIC_ID }}
          LOGS_SERVICE_NAME: 'telegram-bot'
          TELEGRAM_BOT_SERVER_USER: ${{ secrets.TELEGRAM_BOT_SERVER_USER }}
          TELEGRAM_BOT_SERVER_PASSWORD: ${{ secrets.TELEGRAM_BOT_SERVER_PASSWORD }}
          TELEGRAM_BOT_SERVER_HOST: ${{ secrets.TELEGRAM_BOT_SERVER_HOST }}
          TELEGRAM_BOT_SERVER_PORT: ${{ secrets.TELEGRAM_BOT_SERVER_PORT }}
          TELEGRAM_BOT_SERVER_ROOT_PATH: ${{ secrets.TELEGRAM_BOT_SERVER_ROOT_PATH }}
        run: |
          echo "📋 Downloading Telegram Bot logs from ${{ secrets.TELEGRAM_BOT_SERVER_HOST }}..."
          
          # SSH into the server and find all matching log files
          echo "🔍 Searching for log files matching pattern: ~/telegram-bot-logs-*.tar.gz"
          
          echo "🔗 Connecting to server to search for log files..."
          LOG_FILES=$(sshpass -e ssh -o StrictHostKeyChecking=no -o LogLevel=QUIET -o ConnectTimeout=10 -T -p ${{ secrets.TELEGRAM_BOT_SERVER_PORT }} ${{ secrets.TELEGRAM_BOT_SERVER_USER }}@${{ secrets.TELEGRAM_BOT_SERVER_HOST }} 'clear 2>/dev/null || true; find ~ -maxdepth 1 -name "telegram-bot-logs-*.tar.gz" -type f -printf "%T@ %p\n" 2>/dev/null | sort -nr | cut -d" " -f2-' 2>/dev/null || echo "")
          
          echo "🔍 SSH command completed, processing results..."
          
          # Filter out any non-path lines (in case banner still appears)  
          LOG_FILES=$(echo "$LOG_FILES" | grep '^/' || true)
          
          if [ -z "$LOG_FILES" ]; then
            echo "ℹ️  No log files found on server matching pattern: ~/telegram-bot-logs-*.tar.gz"
            echo ""
            echo "💡 Possible solutions:"
            echo "1. Run 'Restart Telegram Bot' workflow first to generate logs"
            echo "2. Check if log files exist with different naming pattern"
            echo "3. Verify the server has been running and generating logs"
            echo ""
            echo "✅ No files to upload - workflow completed successfully"
            exit 0
          fi
          
          # Count the number of log files found
          LOG_COUNT=$(echo "$LOG_FILES" | wc -l)
          echo "📦 Found $LOG_COUNT log file(s):"
          echo "$LOG_FILES"
          
          # Process each log file
          PROCESSED_COUNT=0
          echo "$LOG_FILES" | while IFS= read -r LOG_FILE_PATH; do
            if [ -n "$LOG_FILE_PATH" ]; then
              echo ""
              echo "🚀 Processing log file: $LOG_FILE_PATH"
              
              # Set the log file path for the telegram uploader
              export LOGS_FILE_PATH="$LOG_FILE_PATH"
              
              # Run the telegram uploader script for this file
              node scripts/telegram-logs-uploader.mjs
              
              echo "✅ Completed processing: $LOG_FILE_PATH"
              PROCESSED_COUNT=$((PROCESSED_COUNT + 1))
            fi
          done
          
          echo ""
          echo "📊 Summary: Processed $LOG_COUNT log file(s) successfully"
      
      - name: Notify completion
        if: always()
        run: |
          if [ "${{ job.status }}" == "success" ]; then
            echo "✅ Telegram Bot logs downloaded and uploaded to Telegram successfully"
          else
            echo "❌ Failed to download/upload Telegram Bot logs"
          fi